import React from 'react'
import {Card, Icon, Label} from 'semantic-ui-react'

export default function PostCard(props) {
    const {body, createdAt, id, username, likeCount, commentCount, likes} = props.posts
    return (
        <div>
            
        </div>
    )
}
